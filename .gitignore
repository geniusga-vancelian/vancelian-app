# macOS
.DS_Store

# Python
.venv/
**/.venv/
__pycache__/
*.pyc

# Env (secrets — never commit, only the *.example templates are tracked)
.env
**/.env
.env.arquantix
.env.arquantix.local
.env.local
**/.env.local

# Sauvegardes locales (dump SQL, copies .env) — données sensibles
/backups/

# PDF générés en local (preview design)
docs/arquantix/generated-pdfs/*.pdf

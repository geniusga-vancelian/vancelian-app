<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- HTTP vers le Mac en LAN (même rôle que NSAllowsLocalNetworking côté iOS). -->
    <application android:usesCleartextTraffic="true" />
</manifest>

import { defaultLocale, type Locale } from '@/config/locales'

/** Locale unique pour lire le contenu CMS (vault, blog, catalogue) dans le portail client. */
export const PORTAL_CONTENT_LOCALE: Locale = defaultLocale

/**
 * Jetons couleur — module Funding page offre (Figma).
 * Typographie et espacements : `offerFundingTypography.ts` (`OFFER_FUNDING_*`).
 */
export const offerFundingTokens = {
  /** Accent progression + pourcentage à 100 %. */
  accent: '#527940',
  /** Fond des cartes (Funded / Rate / Total). */
  cardSurface: '#F3F3F3',
  /** Piste de la barre non remplie. */
  progressTrack: '#E0E0E0',
  /** Segment « reste » lorsque `showRemaining` (variante Figma). */
  progressRemainingMuted: 'rgba(98, 101, 110, 0.2)',
} as const

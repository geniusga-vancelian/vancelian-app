import { PortalAuthVerifySkeleton } from '@/components/portal/PortalAuthVerifySkeleton'

/** Contenu formulaire seulement — le shell CMS est déjà fourni par login/layout. */
export default function PortalLoginVerifyLoading() {
  return <PortalAuthVerifySkeleton />
}

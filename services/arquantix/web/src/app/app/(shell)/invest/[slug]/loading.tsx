import { PortalOfferDetailSkeleton } from '@/components/portal/PortalRouteSkeleton'

/** Skeleton streamé pendant le chargement SSR du détail offre / coffre. */
export default function PortalInvestOfferLoading() {
  return <PortalOfferDetailSkeleton />
}

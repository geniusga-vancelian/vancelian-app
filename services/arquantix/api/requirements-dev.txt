# Dépendances de développement / tests pour l'API Arquantix.
#
# **NON installées dans l'image Docker de production** (cf. Dockerfile :
# seul `requirements.txt` est pip install). À installer :
#
#  - localement (uvicorn hôte)         : pip install -r requirements-dev.txt
#  - dans le conteneur (ad-hoc tests)  : docker exec arquantixrecovery-arquantix-api-1 \
#                                          pip install -r /app/requirements-dev.txt
#
# Ces deps sont volontairement séparées pour garder l'image prod minimale.

# ── Test runner & async ───────────────────────────────────────────────
# pytest 9.x : nouvelle major depuis 8.x. Les tests Arquantix sont
# compatibles. pytest-asyncio 1.x utilise la nouvelle API event_loop par
# défaut (loop_scope="function") — cohérent avec nos tests Palier 2 D.2.
pytest==9.0.3
pytest-asyncio==1.3.0

# ── Mocking & couverture ──────────────────────────────────────────────
# (pas strictement requis pour la suite Palier 2 D.2 actuelle qui utilise
# uniquement `unittest.mock` de la stdlib + monkeypatch de pytest, mais
# pratique pour les futurs tests.)
# pytest-mock==3.14.0
# pytest-cov==5.0.0
